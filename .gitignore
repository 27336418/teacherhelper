# OS
.DS_Store
._*

# 助手内部记忆/工作目录（不属于项目源码）
.workbuddy/

# Swift / Xcode / SwiftPM
.build/
.swiftpm/
Package.resolved
*.xcodeproj
*.xcworkspace
DerivedData/
*.xcuserstate
xcuserdata/

# 本机产物（.app 体积大且会随构建重新生成，不进仓库）
教师助手.app/
.dmg-stage-*/

# 安装包：仓库只跟踪「最新一个」
#   jsDelivr 对 /gh/ 仓库有 50MB 总大小上限，堆 8 个旧包（100MB+）会让**新包**被 403 拒发
#   release.sh 会用 `git add -f` 强制把当次发布的 dmg 入库；旧包本地保留，git 历史里也能取回
教师助手*.dmg

# 本机数据目录（不应进仓库；备份导出文件落在此处）
*.bak

# 交付用的介绍材料 / 文档产物（体积大，不进仓库；需要时另行保存）
output/
功能介绍文档/

# 图标原始素材备份与对比图（本机素材，不进仓库）
AppIcon/备份-*/
AppIcon/*对比*.png
