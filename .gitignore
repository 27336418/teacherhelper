# OS
.DS_Store
._*

# 助手内部记忆/工作目录（不属于项目源码）
.workbuddy/

# Swift / Xcode / SwiftPM
.build/
.swiftpm/
Package.resolved
*.xcodeproj
*.xcworkspace
DerivedData/
*.xcuserstate
xcuserdata/

# 本机产物（.app 体积大且会随构建重新生成，不进仓库）
# 发布的 dmg 需要提交进仓库根目录：在线更新从 jsDelivr/raw 直接下载它
教师助手.app/
.dmg-stage-*/

# 本机数据目录（不应进仓库；备份导出文件落在此处）
*.bak

# 交付用的介绍材料 / 文档产物（体积大，不进仓库；需要时另行保存）
output/
功能介绍文档/
