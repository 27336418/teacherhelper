# macOS 顶栏课表应用 — 开发规格文档

## 1. 项目概述

### 1.1 应用名称
**课表** — macOS 顶栏课表管理应用

### 1.2 应用类型
macOS 原生 Swift 应用，以 **菜单栏图标（Status Bar Item）** 形式常驻系统顶栏。

### 1.3 核心定位
一款面向教师的轻量级课表管理工具，支持：
- **个人课表**：展示教师个人的周课时安排（在哪些班级上课/巡班）
- **班级课表**：按班级展示周课表，不同班级用不同颜色区分；**当前仅保留 7班 课表**（8班 暂不需要）

---

## 2. 功能需求

### 2.1 菜单栏入口
| 功能 | 说明 |
|------|------|
| 图标显示 | 系统顶栏显示应用图标（自动选用合适的 SF Symbol，如 `tablecells.fill` / `calendar`，后续可自定义） |
| 面板样式 | 弹出的课表面板为**半透明**（毛玻璃/磨砂效果），适配浅色与深色模式 |
| 点击行为 | 左键点击 → 弹出半透明下拉面板，**优先展示个人课表**（个人课表在面板顶部展开） |
| 退出选项 | **右键点击顶栏图标** → 菜单「退出课表」；面板工具栏亦有「退出」按钮 |
| 布局 | 个人课表与班级课表在**同一列**中垂直排列，个人课表位于最上方 |
| 当前节次高亮 | 根据当前时间自动高亮正在进行的课程/即将开始的课程 |

### 2.2 个人课表视图（默认首屏）
- **数据来源**：个人课表模板格式（见第 5 节）
- **布局**：6 列（**周一~周五 + 周日；周六已移除**）× 12 行（早自习 + 第 1~9 节 + 晚自习）
- **单元格内容**：班级编号或巡班安排（如 `7`、`8`、`巡1-15班`、`8班+巡16-30`）
- **交互**：点击单元格可编辑

### 2.3 班级课表视图
- **数据来源**：班级课表模板格式（见第 5 节），由用户填写后 **导入 Excel**；也支持 **下载/导出** 为 Excel
- **班级范围**：**仅 7班**（8班 暂不需要，界面只展示 7班 一张课表卡片）
- **布局**：6 列（节次 + 星期 1~5）× 多行（分三个时段段）
- **时段分区**：
  - **上午**：节次 一 ~ 五
  - **下午**：节次 六 ~ 九
  - **晚自习**：节次 1 ~ 4
- **分隔行**：**已移除**（大课间操 / 中午休息 / 眼保健健康 等不再显示，节次直接排列）
- **单元格内容**：仅班级编号（`7`），空单元格表示该时段无课；**支持直接编辑**
- **颜色编码**：依据单元格内容判定班级 → 7班 底色（见 2.5）

### 2.4 课表管理功能
| 功能 | 说明 |
|------|------|
| 导入 Excel | 支持 `.xlsx` 格式，自动识别个人/班级课表模板，写入对应数据集 |
| 下载/导出 | 将当前课表导出为 `.xlsx` 文件（个人课表、7班课表均可单独下载） |
| 提供模板 | 内置「7班课表模板.xlsx」供用户填写后导入 |
| 直接修改 | 在界面内点单元格直接输入编辑 |
| 新建课表 | 清空当前个人课表（或 7班课表）重新填写 |

### 2.5 颜色方案（按班级）
课表内容仅涉及 **7班** 与 **8班**，颜色编码以**班级**为单位（而非科目）：

| 班级 | 判定规则 | 建议色 | 色值参考 |
|------|----------|--------|----------|
| 7班 | 单元格文本包含 `7`（且不包含 `8`） | 蓝绿 | #16A085 |
| 8班 | 单元格文本包含 `8` | 橙红 | #E67E22 |
| 巡班（无 7 无 8） | 如 `巡1-15班`、`单7/双巡16-30` 中的纯巡班部分 | 中性灰 | #95A5A6 |
| 空 | 无内容 | 透明 / 浅灰 | — |

**匹配优先级**：
1. 文本包含 `8` → 判为 **8班**（橙红）
2. 否则文本包含 `7` → 判为 **7班**（蓝绿）
3. 否则（如纯巡班 `巡1-15班`）→ **中性灰**

> 说明：多数单元格只含单一班级（如 `7`、`8`、`8班+巡16-30`），规则直接命中；个别同时含 7 与 8 的混合描述（如 `8班+巡16-30`）按主班级优先判为 8班。颜色需同时支持浅色/深色模式适配。

### 2.6 数据持久化
- 本地 JSON / SQLite 存储所有课表数据
- 支持多份课表同时管理（多班级 + 个人）
- 自动保存，无需手动 Ctrl+S

---

## 3. UI/UX 设计

### 3.1 信息架构

```
菜单栏图标（半透明面板）
└── 点击弹出 Popover（毛玻璃磨砂背景，半透明）
    ├── 顶部工具栏
    │   ├── [导入] [导出] [新建] 按钮
    │   └── 当前时间 + 当前节次指示
    │
    └── 单列垂直滚动区（个人课表在上，班级课表在下）
        ├── 📌 个人课表卡片（默认展开，优先展示）
        │   ├── 表头：星期一 ~ 星期日
        │   ├── 左侧列：节次标签（早自习/第1~9节/晚自习）
        │   └── 单元格网格（7班蓝绿 / 8班橙红 / 巡班灰）
        │
        └── 🏫 7班 课表卡片（单列，位于个人下方）
            └── 表头：星期1 ~ 星期5 + 节次标签（上午/下午/晚自习分段）
```

> 个人课表与 7班 课表同在**一列**中，向下滚动即可查看。

### 3.2 面板尺寸与外观
- **面板背景**：半透明磨砂（NSVisualEffectView，material 取 `.popover` 或 `.hudWindow`，blendingMode `.withinWindow`）
- **个人课表卡片**：约 580 × 420 px
- **班级课表卡片**：约 520 × 600 px（因行数较多）
- 单列整体可滚动，面板最大高度约 700 px 后内部滚动
- 支持浅色/深色模式自动适配

### 3.3 交互细节
| 操作 | 响应 |
|------|------|
| 单击空单元格 | 进入编辑模式（文本输入/下拉选择） |
| 单击有内容的单元格 | 选中（高亮边框），再次单击编辑 |
| 双击任意单元格 | 直接编辑 |
| 右键单元格 | 上下文菜单（复制、粘贴、清空、合并/拆分） |
| 拖拽选择 | 多选单元格，批量操作 |
| ESC | 取消编辑 |
| Enter | 确认编辑，移动到下一格 |

### 3.4 当前节次高亮逻辑
```
根据系统当前时间 → 匹配课表中的时间段 → 高亮对应行
- 早自习：07:00 ~ 07:40
- 第1节：07:50 ~ 08:35
- 第2节：08:45 ~ 09:30
- 大课间操：09:30 ~ 09:55
- 第3节：09:55 ~ 10:40
- 第4节：10:50 ~ 11:35
- 第5节：11:45 ~ 12:30
- 中午休息：12:30 ~ 14:00
- 第6节：14:00 ~ 14:45
- 眼保健健康：14:45 ~ 14:55
- 第7节：14:55 ~ 15:40
- 第8节：15:50 ~ 16:35
- 第9节：16:45 ~ 17:30
- 晚自习1：18:30 ~ 19:10
- 晚自习2：19:20 ~ 20:00
- 晚自习3：20:10 ~ 20:50
- 晚自习4：21:00 ~ 21:40
```
> 具体时间节点可在设置中自定义。

---

## 4. 技术方案

### 4.1 技术栈
| 组件 | 选型 | 说明 |
|------|------|------|
| 语言 | Swift 5.9+ | macOS 原生开发 |
| UI 框架 | SwiftUI + AppKit 桥接 | SwiftUI 为主，菜单栏项用 NSStatusItem |
| Excel 解析 | CoreXLSX 或 LibreOffice SDK | 读取 .xlsx 文件 |
| Excel 导出 | XLSXWriter (Swift) 或调用系统 API | 导出 .xlsx 文件 |
| 数据存储 | SwiftData / CoreData | 课表数据的本地持久化 |
| 最低系统 | macOS 13.0 (Ventura) | 覆盖大部分活跃设备 |

### 4.2 项目结构（建议）
```
ScheduleBar/
├── ScheduleBarApp.swift          # App 入口
├── Models/
│   ├── Schedule.swift            # 课表数据模型
│   ├── ScheduleType.swift        # 课表枚举（个人/班级）
│   ├── ScheduleCell.swift        # 单元格模型
│   └── ClassInfo.swift           # 班级模型（7班/8班，含颜色）
├── ViewModels/
│   ├── ScheduleViewModel.swift   # 课表业务逻辑
│   ├── ImportExportViewModel.swift # 导入导出逻辑
│   └── TimeMatcher.swift         # 当前节次匹配
├── Views/
│   ├── MenuBarView.swift         # 菜单栏图标 & 面板
│   ├── PersonalScheduleView.swift # 个人课表视图
│   ├── ClassScheduleView.swift   # 班级课表视图
│   ├── ScheduleTableView.swift   # 课表表格组件
│   ├── ScheduleCellView.swift    # 单元格视图
│   └── ToolbarView.swift         # 工具栏
├── Services/
│   ├── ExcelImporter.swift       # Excel 导入服务
│   ├── ExcelExporter.swift       # Excel 导出服务
│   └── ScheduleStorage.swift     # 数据持久化
├── Resources/
│   ├── Colors.swift              # 班级颜色定义（7班/8班）
│   └── TimeConfig.swift          # 节次时间配置
├── Assets.xcassets               # 图标资源
└── ScheduleBar.entitlements      # 权限声明
```

### 4.3 关键技术点

#### 4.3.1 菜单栏常驻实现
```swift
// 使用 NSStatusBar + NSStatusItem
class MenuBarController: NSObject, ObservableObject {
    private var statusItem: NSStatusItem?
    private var popover: NSPopover?

    func setupStatusItem() {
        statusItem = NSStatusBar.system.statusItem(withLength: NSStatusItem.squareLength)
        if let button = statusItem?.button {
            button.image = NSImage(systemSymbolName: "tablecells.fill", accessibilityDescription: "课表")
            button.action = #selector(togglePopover)
        }
    }
}
```

#### 4.3.2 Excel 导入解析流程
```
选择 .xlsx 文件
  → 用 CoreXLSX 解析工作表
  → 检测模板类型（个人 / 班级）：
      - 个人：表头含"周一~周五、周日"（6 列），行标签为"早自习/第N节/晚自习"
      - 班级：表头含"星期1~5"，行标签含"一~九"及"上午/下午/晚自习"
  → 映射到内部数据模型
  → 存储到本地数据库
  → 刷新 UI
```

#### 4.3.3 班级课表特殊处理
班级课表（v0.4 起）**不再包含**「大课间操 / 中午休息 / 眼保健健康」分隔行：
- 「上午」「下午」「晚自习」分组行保留，仅节次单元格填写班级号
- 若导入旧模板（仍含分隔行），导入逻辑会自动忽略这些行

---

## 5. 课表模板格式规范

### 5.1 个人课表模板（导入格式）

**文件示例**：`/Users/a123/Desktop/个人课表模板.xlsx`

| | A（节次） | B（周一） | C（周二） | D（周三） | E（周四） | F（周五） | G（周六） | H（周日） |
|---|---|---|---|---|---|---|---|---|---|
| **1** | 节次 | 周一 | 周二 | 周三 | 周四 | 周五 | 周六 | 周日 |
| **2** | 早自习 | *内容* | ... | ... | ... | ... | ... | ... |
| **3** | 第1节 | *内容* | ... | ... | ... | ... | ... | ... |
| ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |
| **12** | 晚自习 | *内容* | ... | ... | ... | ... | ... | ... | ... |

**特征**：
- 无合并单元格
- 7 列 × 12 行（含表头；**已去掉周六**）
- 列顺序：周一 → 周二 → 周三 → 周四 → 周五 → 周日
- 单元格值：班级号（数字）、巡班描述、或空
- 兼容：旧模板（8 列含周六）导入时自动忽略周六列

### 5.2 班级课表模板（导入格式）

**文件示例**：`7班课表模板.xlsx`（由应用提供，用户填写后导入）

**结构特征**：
- 6 列（节次 + 星期 1~5），共 17 行（表头 + 3 分组行 + 13 节次行）
- 分组行（上午/下午/晚自习）留空，仅节次单元格填写
- **无分隔行**（大课间操 / 中午休息 / 眼保健健康 已移除）
- 行分组：

```
┌─────────────────────────────────────┐
│  节次  │ 星期1 │ 星期2 │ ... │ 星期5 │
├─────────┼───────┼───────┼─────┼───────┤
│ 上午 ──┬─ 一    │  7   │  8   │ ... │  7   │
│        ├─ 二    │  7   │  8   │ ... │  7   │
│        ├─ 三    │  7   │  8   │ ... │  8   │
│        ├─ 四    │  8   │  7   │ ... │  8   │
│        └─ 五    │  7   │  8   │ ... │  7   │
├─────────┼───────┼───────┼─────┼───────┤
│ 下午 ──┬─ 六    │  8   │  7   │ ... │  8   │
│        ├─ 七    │  7   │  8   │ ... │  7   │
│        ├─ 八    │  8   │      │ ... │  7   │
│        └─ 九    │  7   │      │ ... │  8   │
├─────────┼───────┼───────┼─────┼───────┤
│晚自习─┬─ 1      │  7   │  8   │ ... │  7   │
│       ├─ 2      │  7   │  8   │ ... │  7   │
│       ├─ 3      │  8   │  7   │ ... │  8   │
│       └─ 4      │      │      │ ... │      │
└─────────────────────────────────────┘
```

**单元格格式**：每格仅含一个班级编号（`7` 或 `8`），空单元格表示无课。
**颜色判定**：单元格文本包含 `7` → 7班色；包含 `8` → 8班色（见 2.5）。

---

## 6. 开发里程碑

| 阶段 | 内容 | 交付物 |
|------|------|--------|
| **M1** | 项目脚手架 + 菜单栏图标 + 空白面板 | 可运行的空壳 app |
| **M2** | 数据模型 + 个人课表视图 + 本地存储 | 可查看/编辑个人课表 |
| **M3** | 7班 班级课表视图 + 颜色编码 | 完整的 7班 课表功能 |
| **M4** | Excel 导入（.xlsx 解析，自动识别个人/班级） | 可从模板文件导入课表 |
| **M5** | Excel 导出/下载 + 模板提供 + 编辑优化 | 完整的导入导出和编辑体验 |
| **M6** | 当前节次高亮 + 时间匹配 + 打磨 UI | 发布前打磨 |
| **M7** | DMG 打包分发 | 可安装的 .dmg 安装包 |

---

## 7. 已确认事项 & 待确认事项

### ✅ 已确认（来自用户澄清）
1. **应用名称**：「课表」
2. **图标**：自动选用合适的 SF Symbol（如 `tablecells.fill`），后续可自定义
3. **面板样式**：半透明（毛玻璃/磨砂效果）
4. **布局**：个人课表与班级课表在**同一列**垂直排列，个人课表优先展开在顶部
5. **颜色逻辑**：按班级而非科目 —— 含 `7` → 7班色，含 `8` → 8班色
6. **班级范围**：班级课表**只保留 7班**（8班 暂不需要，界面仅一张 7班 卡片）
7. **导入/下载**：提供「7班课表模板.xlsx」供填写后导入；支持将课表导出/下载为 `.xlsx`
8. **推进**：先出预览 Demo（已完成 M1+M2），现补充 M3+M4+M5（7班视图 + 导入/导出）

### ❓ 待确认（实现前需明确）
1. **节次时间**：第 3.4 节的作息时间表是否符合你学校实际？是否调整？（这直接影响"当前节次高亮"）
2. **是否需要通知提醒**：下节课开始前是否弹出系统通知（如提前 2 分钟）？还是仅高亮即可？

---

*文档版本：v0.3 | 更新日期：2026-09-07 | 状态：待评审*
